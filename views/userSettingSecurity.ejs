<!DOCTYPE html>
<html lang="en">
<<<<<<< HEAD
    <head>
        <meta charset="UTF-8" />
        <title>Account Security</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
        />
        <link rel="stylesheet" href="/css/userSetting.css" />
        <style>
            .mobile-menu-button {
                background: none;
                border: none;
                padding: 0;
                margin-right: 10px;
                cursor: pointer;
                display: flex;
                flex-direction: column;
                justify-content: center;
                gap: 5px;
                width: 30px;
                height: 30px;
            }
            .mobile-menu-button span {
                background-color: #000 !important;
                display: block;
                width: 24px;
                height: 3px;
                border-radius: 2px;
            }
            .mobile-menu-button span {
                background-color: #000; /* Black for light mode */
            }

            body.dark .mobile-menu-button span {
                background-color: #fff; /* White for dark mode */
            }

            body.dark .mobile-menu-button span {
                background-color: #fff;
                background-color: white; /* Dark mode hamburger lines */
            }
=======
<head>
  <meta charset="UTF-8" />
  <title>Account Security</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/userSetting.css" />
  <style>
    /* Base styles */
    body {
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    /* Light mode (default) */
    body {
      background-color: #ffffff;
      color: #212529;
    }

    /* Dark mode styles using your color palette */
    body.dark {
      background-color: #1B2B30; /* Dark blue-gray for background */
      color: #E1E6F0; /* Light gray-blue for text */
    }

    /* Mobile menu button */
    .mobile-menu-button {
      background: none;
      border: none;
      padding: 0;
      margin-right: 10px;
      cursor: pointer;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 5px;
      width: 30px;
      height: 30px;
    }

    .mobile-menu-button span {
      background-color: #212529; /* Dark for light mode */
      display: block;
      width: 24px;
      height: 3px;
      border-radius: 2px;
      transition: background-color 0.3s ease;
    }
    
    body.dark .mobile-menu-button span {
      background-color: #E1E6F0; /* Light gray-blue for dark mode */
    }
    
    /* Dropdown Sidebar */
    #sidebarDropdown {
      background-color: #ffffff;
      border-right: 1px solid #e0e0e0;
      height: 100%;
      overflow-y: auto;
      padding: 20px 0;
      transition: all 0.3s ease-in-out;
    }
    
    body.dark #sidebarDropdown {
      background-color: #1B2B30; /* Dark blue-gray */
      border-right: 1px solid #397CA2; /* Medium blue for borders */
    }
    
    #sidebarDropdown .nav {
      margin-top: 20px;
    }
    #sidebarDropdown .nav-link {
      padding: 10px 20px;
      font-size: 1.1rem;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    
    body.dark #sidebarDropdown .nav-link {
      color: black; /* Light blue for links in dark mode */
    }
    
    #sidebarDropdown .nav-item {
      margin-bottom: 20px;
    }
    
    @media (max-width: 767px) {
      #sidebarDropdown {
        position: absolute;
        top: 56px;
        left: -260px;
        width: 250px;
        height: 100%;
        z-index: 2000;
      }
      #sidebarDropdown.open {
        left: 0;
      }
    }
    
    /* Highlight color for active items */
    .bg-orange {
      background-color: #F79A7C !important; /* Peach/salmon color from your palette */
      color: #1B2B30 !important; /* Dark background color for contrast */
    }
    
    body.dark .bg-orange {
      background-color: #F79A7C !important; /* Same peach color in dark mode */
      color: #1B2B30 !important; /* Dark background color for contrast */
    }
    
    /* Mobile sidebar */
    #leftMobileSidebar {
      background-color: #fdfdfd;
      color: #212529;
      width: 260px;
      transform: translateX(-100%);
      transition: transform 0.3s ease-in-out, background-color 0.3s ease, color 0.3s ease;
      padding: 24px 16px;
      z-index: 1050;
      border-right: 1px solid #e0e0e0;
    }
    
    /* Dark Mode overrides for mobile sidebar */
    body.dark #leftMobileSidebar {
      background-color: #1B2B30; /* Dark blue-gray */
      color: #E1E6F0; /* Light gray-blue for text */
      border-right: 1px solid #397CA2; /* Medium blue for borders */
    }
    
    #leftMobileSidebar .nav-link {
      padding: 12px 18px;
      margin-bottom: 12px;
      border-radius: 8px;
      color: #212529;
      transition: background 0.2s ease, color 0.2s ease;
    }
    
    body.dark #leftMobileSidebar .nav-link {
      color: #A8C7E7 !important; /* Light blue for links */
    }
    
    body.dark #leftMobileSidebar .nav-link:hover {
      background-color: rgba(57, 124, 162, 0.2); /* Subtle hover effect with medium blue */
    }
    
    body.dark #leftMobileSidebar .nav-link.bg-orange {
      background-color: #F79A7C !important; /* Peach/salmon color */
      color: #1B2B30 !important; /* Dark background for contrast with the peach */
    }
    
    #leftMobileSidebar .text-center {
      margin-bottom: 24px;
    }
    
    #leftMobileSidebar.open {
      transform: translateX(0);
    }
    
    body.dark #leftMobileSidebar h6 {
      color: #E1E6F0 !important; /* Light gray-blue for text */
    }

    /* Additional styles for main content area in dark mode */
    body.dark main {
      background-color: #1B2B30; /* Dark blue-gray */
      color: #E1E6F0; /* Light gray-blue for text */
    }

    /* Card styling for dark mode */
    body.dark .card {
      background-color: #263940; /* Slightly lighter than the main background */
      border-color: #397CA2; /* Medium blue for borders */
    }

    body.dark .card-header {
      background-color: rgba(57, 124, 162, 0.3); /* Semi-transparent medium blue */
      border-bottom-color: #397CA2; /* Medium blue for borders */
    }

    /* Form controls in dark mode */
    body.dark .form-control {
      background-color: #1B2B30;
      border-color: #397CA2;
      color: #E1E6F0;
    }

    body.dark .form-control:focus {
      background-color: #263940;
      border-color: #A8C7E7;
      color: #E1E6F0;
      box-shadow: 0 0 0 0.25rem rgba(168, 199, 231, 0.25);
    }

    /* Buttons in dark mode */
    body.dark .btn-primary {
      background-color: #397CA2;
      border-color: #397CA2;
    }

    body.dark .btn-primary:hover {
      background-color: #306A8C;
      border-color: #306A8C;
    }

    body.dark .btn-outline-secondary {
      color: #A8C7E7;
      border-color: #A8C7E7;
    }

    body.dark .btn-outline-secondary:hover {
      background-color: #A8C7E7;
      color: #1B2B30;
    }
  </style>
</head>
<body class="<%= user.theme %>" data-user-id="<%= user._id %>">
>>>>>>> payment

            @media (min-width: 768px) {
                .mobile-menu-button {
                    display: none;
                }
            }

            /* Dropdown Sidebar */
            #sidebarDropdown {
                background-color: #000000;
                border-right: 1px solid #000000;
                height: 100%;
                overflow-y: auto;
                padding: 20px 0;
                transition: left 0.3s ease-in-out;
            }

            #sidebarDropdown .nav {
                margin-top: 20px;
            }

            #sidebarDropdown .nav-link {
                padding: 10px 20px;
                font-size: 1.1rem;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }

            #sidebarDropdown .nav-item {
                margin-bottom: 20px;
            }

            @media (max-width: 767px) {
                #sidebarDropdown {
                    position: absolute;
                    top: 56px;
                    left: -260px;
                    width: 250px;
                    height: 100%;
                    z-index: 2000;
                }
                #sidebarDropdown.open {
                    left: 0;
                }
            }

            .bg-orange {
                background-color: #e07b4f !important;
            }

            /* Default light mode styles */
            #leftMobileSidebar {
                background-color: #fdfdfd;
                color: #212529;
                width: 260px;
                transform: translateX(-100%);
                transition: transform 0.3s ease-in-out;
                padding: 24px 16px;
                z-index: 1050;
                border-right: 1px solid #e0e0e0;
            }

            /* Dark Mode overrides */
            body.dark #leftMobileSidebar {
                background-color: #1f1f1f;
                color: #f5f5f5;
                border-right: 1px solid #333;
            }

            #leftMobileSidebar .nav-link {
                padding: 12px 18px;
                margin-bottom: 12px;
                border-radius: 8px;
                color: #212529;
                transition: background 0.2s ease;
            }

            #leftMobileSidebar .text-center {
                margin-bottom: 24px;
            }

            #leftMobileSidebar.open {
                transform: translateX(0);
            }

            body.dark #leftMobileSidebar .nav-link {
                color: #ccc !important;
            }

            body.dark #leftMobileSidebar .nav-link.bg-orange {
                background-color: #f3c6a0 !important;
                color: #1f1f1f !important;
            }

            body.dark #leftMobileSidebar h6 {
                color: white !important;
            }
        </style>
    </head>
    <body class="<%= user.theme %>" data-user-id="<%= user._id %>">
        <div class="container-fluid">
            <%- include('partials/navbar') %>

            <div class="row flex-grow-1 min-vh-100">
                <!-- Sidebar -->
                <%- include('partials/userSettings/sideBar') %>

                <!-- Main Content -->
                <main class="col-md-10 col-12 p-4">
                    <%- include('partials/userSettings/userSecurity', { user,
                    activePage, success, error }) %>
                </main>
            </div>
        </div>

        <script src="/js/userSecurity.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
