<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Account Security</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/userProfile.css" />
</head>

<body data-theme="<%= user.theme %>">
  <!-- navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
    <a class="navbar-brand" href="#">YourSite</a>
    <div class="ms-auto">
      <span class="text-white me-3">Welcome, <%= user.firstName %></span>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row vh-100">

      <!-- sidebar -->
      <%- include('partials/sideBar') %>

      <!-- main Content -->
      <main class="col-md-10 p-4">
        <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
          <div class="text-muted fs-5">←</div>
          <h1 class="h4 m-0">Account Security</h1>
          <div></div>
        </div>

        <!-- form -->
        <form action="/profile/security/update" method="POST" class="row">
          <div class="col-md-8">
            <!-- email -->
            <div class="mb-3 d-flex align-items-center gap-3">
              <input type="email" name="email" class="form-control" value="<%= user.email %>" placeholder="User Email" required />
              <button type="submit" formaction="/profile/security/change-email" class="btn btn-outline-danger">Change Email</button>
            </div>

            <hr class="my-4" />

            <!-- password -->
            <div class="mb-3">
              <input type="password" name="oldPassword" class="form-control" placeholder="Enter old password" required />
            </div>
            <div class="mb-3">
              <input type="password" name="newPassword" class="form-control" placeholder="Enter new password" required />
            </div>
            <div class="mb-3">
              <input type="password" name="confirmPassword" class="form-control" placeholder="Re-type new password" required />
            </div>

            <button type="submit" formaction="/profile/security/change-password" class="btn btn-outline-danger float-end mt-2">Change password</button>
          </div>
        </form>
      </main>
    </div>
  </div>
</body>
</html>