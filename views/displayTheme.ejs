<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Display</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/userProfile.css" />
</head>
<body data-theme="<%= user.theme %>">
    <!--  navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
    <a class="navbar-brand" href="#">YourSite</a>
    <div class="ms-auto">
      <span class="text-white me-3">Welcome, <%= user.firstName %></span>
    </div>
  </nav>
  <div class="container-fluid">
    <div class="row vh-100">


       <!-- sidebar -->
       <%- include('partials/sideBar') %>
       
      <!-- main Content -->
      <main class="col-md-10 p-4">
      <!-- header -->
      <div class="d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
        <div class="text-muted fs-5">←</div>
        <h1 class="h4 m-0">Display Settings</h1>
        <div class="d-flex gap-3 fs-5"></div>
      </div>

      <!-- form -->
      <form action="/profile/display/update" method="POST" class="row">
        <div class="col-md-8">
          <div class="mb-4">
            <h5 class="fw-bold">Choose your theme</h5>
            <div class="form-check">
              <input class="form-check-input " type="radio" name="theme" id="lightMode" value="light" <%= user.theme === 'light' ? 'checked' : '' %>>
              <label class="form-check-label" for="lightMode">Light Mode</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="theme" id="darkMode" value="dark" <%= user.theme === 'dark' ? 'checked' : '' %>>
              <label class="form-check-label" for="darkMode">Dark Mode</label>
            </div>
          </div>
        </div>

        <div class="col-md-4 d-flex justify-content-end align-items-start pt-2">
          <button type="submit" class="btn btn-outline-danger">Save</button>
        </div>
      </form>

    </main>

    </div>
</div>
</main>
</body>
</html>